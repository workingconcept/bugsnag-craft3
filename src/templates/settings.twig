{% import "_includes/forms" as forms %}

{{ forms.textField({
    first: true,
    label: "Bugsnag API Key",
    instructions: "Add the relevant Bugsnag ID for this project.",
    name: 'apiKey',
    required: true,
    value: settings.apiKey
}) }}
